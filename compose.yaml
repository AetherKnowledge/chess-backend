services:
  postgres:
    container_name: chess-db
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=chess'
      - 'POSTGRES_PASSWORD=root'
      - 'POSTGRES_USER=root'
    ports:
      - '5432:5432'
